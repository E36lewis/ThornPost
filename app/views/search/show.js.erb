$('[data-behavior="endless-scroll"]').append('<%= j render @stories %>');

ReactRailsUJS.mountComponents();

<% if @post_records.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @stprie_records %>');
<% else %>
  $('.pagination').remove();
<% end %>
