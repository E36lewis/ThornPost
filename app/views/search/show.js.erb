$('[data-behavior="endless-scroll"]').append('<%= j render @stories %>');

ReactRailsUJS.mountComponents();

<% if @story_records.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @story_records %>');
<% else %>
  $('.pagination').remove();
<% end %>
