$('[data-behavior="endless-scroll"]').append('<%= j render @stories %>');

ReactRailsUJS.mountComponents();

<% if @storie_records.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @storie_records %>');
<% else %>
  $('.pagination').remove();
<% end %>
